---
import Splash from '../../features/splash/index.astro';
import { getLocaleStaticPaths } from '../_shared/utils';
export const getStaticPaths = getLocaleStaticPaths;

interface Props {
  locale: string;
}

const { locale } = Astro.props;
---
<Splash locale={locale} />
